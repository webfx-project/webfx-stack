# Which mail transport provider transmits emails: none | smtp | sandbox (later: aws-ses).
# 'none' (default) disables transmission entirely — the safe state for dev builds.
# Staging selects 'sandbox' so a prod→staging refresh can never email real customers.
provider = ${{ MAIL_TRANSPORT_PROVIDER | 'none' }}
