<?xml version="1.0" encoding="UTF-8"?>
<project>

    <description>SMTP implementation of the mail transport SPI, built on the Vert.x mail
        client. The SMTP route (host/port/ssl/credentials) is resolved PER MESSAGE from the
        TransportTags carried by the TransportMessage — in Modality these come from the
        per-account SmtpAccount DB rows — with optional config fallbacks
        (webfx.stack.mail.transport.smtp.defaultHost etc.) for deployments with a single
        fixed relay. Mail clients are cached per route. Selected at runtime by
        webfx.stack.mail.transport.provider = smtp.</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <providers>
        <!-- Static registration is fine here: the provider constructor is config-free;
             the transport booter initializes the one provider selected by config. -->
        <provider interface="dev.webfx.stack.mail.transport.spi.MailTransportProvider">dev.webfx.stack.mail.transport.spi.impl.smtp.vertx.SmtpVertxMailTransportProvider</provider>
    </providers>

</project>
