{
  "httpServers": [
    {
      "port": "${{ HTTPS_PORT | 443 }}",
      "certPath": "${{ CERT_PATH }}",
      "keyPath": "${{ KEY_PATH }}"
    },
    {
      "port": "${{ HTTP_PORT | 8080 }}"
    }
  ]
}