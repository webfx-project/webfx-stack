{
  "httpServers": [
    {
      "protocol": "https",
      "port": "${{ HTTPS_PORT | 443 }}",
      "certPath": "${{ CERT_PATH }}",
      "keyPath": "${{ KEY_PATH }}"
    },
    {
      "protocol": "http",
      "port": "${{ HTTP_PORT | 80 }}"
    }
  ]
}